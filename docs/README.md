# Docs

The rendered guide lives in [`index.md`](./index.md).

GitHub can render the main guide directly from the repository `README.md`. The `docs/` copy exists so the repository can also be used with GitHub Pages or other static documentation tools later.

